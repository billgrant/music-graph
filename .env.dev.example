# Dev Environment Variables
# Copy this to .env.dev on the VM
# NEVER commit .env.dev to git!

# Note: DATABASE_URL and SECRET_KEY are fetched from GCP Secret Manager
# at container startup (see entrypoint.sh). No need to set them here.

# Application Settings
ENVIRONMENT=dev
FLASK_ENV=development
FLASK_DEBUG=1
