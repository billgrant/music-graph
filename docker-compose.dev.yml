services:
  web:
    image: gcr.io/music-graph-479719/music-graph:dev-latest
    env_file:
      - .env.dev
    network_mode: host  # Required for GCP metadata server access
    restart: unless-stopped
    environment:
      - ENVIRONMENT=dev
      - FLASK_ENV=development
      - FLASK_DEBUG=1
